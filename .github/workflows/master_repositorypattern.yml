# Docs for the Azure Web Apps Deploy action: https://github.com/Azure/webapps-deploy
# More GitHub Actions for Azure: https://github.com/Azure/actions

name: Build and deploy container app to Azure Web App - repositorypattern

on:
  push:
    branches:
      - master
  workflow_dispatch:

jobs:
  build:
    runs-on: 'ubuntu-latest'

    steps:
    - uses: actions/checkout@v2

    - name: Set up Docker Buildx
      uses: docker/setup-buildx-action@v2

    - name: Log in to registry
      uses: docker/login-action@v2
      with:
        registry: https://index.docker.io/v1/
        username: ${{ secrets.AzureAppService_ContainerUsername_f239c43899a343dbb250c09b959fd040 }}
        password: ${{ secrets.AzureAppService_ContainerPassword_71f12e9c0cf94de0960043a2adf769a4 }}

    - name: Build and push container image to registry
      uses: docker/build-push-action@v3
      with:
        push: true
        tags: index.docker.io/${{ secrets.AzureAppService_ContainerUsername_f239c43899a343dbb250c09b959fd040 }}/hansadrianus/repository-pattern:${{ github.sha }}
        file: ./API//Dockerfile

  deploy:
    runs-on: ubuntu-latest
    needs: build
    environment:
      name: 'production'
      url: ${{ steps.deploy-to-webapp.outputs.webapp-url }}

    steps:
    - name: Deploy to Azure Web App
      id: deploy-to-webapp
      uses: azure/webapps-deploy@v2
      with:
        app-name: 'repositorypattern'
        slot-name: 'production'
        publish-profile: ${{ secrets.AzureAppService_PublishProfile_fbe714fd172545d8a39fd1d35bf6afa2 }}
        images: 'index.docker.io/${{ secrets.AzureAppService_ContainerUsername_f239c43899a343dbb250c09b959fd040 }}/hansadrianus/repository-pattern:${{ github.sha }}'
